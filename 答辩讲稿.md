# AI 面试题练习器 — 答辩讲稿

> 总时长：约 8-10 分钟 | 语气：真实、朴素、小白视角

---

## 一、开场（40 秒）

各位老师好，我是 XXX。

说实话，一个月前我对计算机专业几乎没什么了解。8 月 17 号开始，我才第一次认真学习打字指法，第一次安装 VS Code，第一次知道什么是 Markdown。然后学长给了我一堆学习资料，让我从了解"计算机专业到底是什么"开始，一步一步学。

今天给大家展示的「AI 面试题练习器」，就是我在这个月学完基础之后，用 AI 编程工具做的第一个项目。虽然代码不是我一行一行手写的，但做什么、怎么做、对不对，都是我来决定的。

---

## 二、我的学习经历（1.5 分钟）

### 第一周：从零开始

第一周我做的事可能很多同学觉得不可思议——我在学打字。学长给的练习网站是 dazidazi.com，要求严格遵循指法。然后我了解了计算机专业的各个方向：前端、后端、测试、运维，在 BOSS 直聘上看岗位要求。注册了 CSDN、牛客账号，安装了 VS Code、Typora，学了 Markdown 语法，了解了 JSON 数据格式。

说实话第一周信息量很大，很多东西看了就忘，但至少建立了一个整体认识。

### 第二周：搭建个人博客

第二周学长让我用 VitePress 框架搭一个个人博客。这是我第一次接触"项目"的概念——安装 Node.js、设置国内镜像源、用 npm 装依赖、搭脚手架、写文章、做主题美化。每开发完一版就 git commit 一次，第一次体会到版本管理的作用。

最后用 GitHub Pages 部署上线，写了 GitHub Action 工作流文件，实现了代码推送就自动部署。当时看到自己的网站真的能从浏览器访问，那个感觉特别神奇。

### 第三周：AI 编程任务

博客搭完之后，学长给了新的任务：学习 Claude Code，了解 CC Switch、MCP、Skill、Memory 等概念，然后用 AI 编程的方式开发一个作品，30 号答辩。

这就进入了一个全新的领域。

---

## 三、AI 工具学习过程（1.5 分钟）

### 什么是 Claude Code？

Claude Code 是一个 AI 编程工具，你可以用自然语言跟它说话，比如"帮我加一个搜索功能"，它就会帮你写代码。跟 ChatGPT 的区别是，Claude Code 能直接读写你电脑上的文件，不只是聊天。

### 遇到的第一个坑：网络问题

Claude Code 需要访问 Claude 的服务器，但国内网络访问不了。我折腾了很久，最后学长告诉我可以用智谱开放平台的 GLM 模型替代，通过国内 API 端点访问，不需要翻墙，还有免费额度。

### CC Switch 配置

CC Switch 是一个图形化的模型切换工具。我在里面配置了智谱的 API Key 和 Base URL，把模型从 Claude 切换到 GLM-4-Flash。中间踩了很多坑——API Key 复制不完整导致 401 认证失败、PowerShell 执行策略限制脚本运行、中文输入乱码需要设置 UTF-8 编码。

### MCP 和 Skill 是什么？

这两个概念一开始我没看懂，因为是英文文档。后来用大白话理解了：

- **MCP**（模型上下文协议）：就是一个标准，让 AI 能访问外部系统，比如文件系统、数据库。相当于给 AI 装了"手"，能操作你电脑上的东西。
- **Skill**：就是可复用的提示词模板，类似函数。写好一个 Skill，以后用一句话就能触发一整套流程。

---

## 四、项目开发（3 分钟）

### 为什么做面试题练习器？

学长说"按你的想法 vibe 一个作品出来"。我想了一下，自己正在准备面试，不如做一个面试题练习器，既能学习 AI 编程，又能帮自己复习。

### 性能优化方面（重点讲）

虽然我是小白，但在开发过程中学到了一些性能方面的知识：

**数据层：零网络请求。** 我把 50 道题直接写在 JS 文件里，不用 fetch 请求外部文件。原因是本地打开 HTML 时浏览器会拦截跨域请求（CORS 问题），而且网络请求有延迟。数据跟着代码一起加载，打开就能用。

**本地存储：localStorage。** 所有用户数据——错题记录、收藏、连击、积分、答题用时——都存在浏览器本地。读取是毫秒级的，不需要后端数据库，刷新页面不丢失。

**动画优化：用 transform 不用 top/left。** 这个是 AI 告诉我的，CSS 动画用 `transform` 和 `opacity` 不会触发浏览器重排，会被 GPU 加速。如果用 `top`、`left` 会卡。虽然我一开始不太懂为什么，但查阅资料后理解了浏览器渲染流程。

**计时器防泄漏。** 答题计时器用 setInterval 实现，每次进入新题目前先清除上一个计时器。AI 说如果不清除，多个计时器同时运行会导致内存泄漏。这个我测试过，确实不加 clearInterval 的话 CPU 占用会变高。

### 功能快速演示

接下来快速演示一下主要功能：

- 点击"开始答题"进入顺序答题，红色闹钟计时器实时计时
- 答对弹贴纸动画，答错弹安慰贴纸
- 连对 3 题触发撒花特效，连错 3 题全屏变灰抖动
- 错题本自动记录，答对自动移除
- 统计页有正确率饼图、用时分布图、积分等级

### 开发模式

整个过程中，AI 负责写代码，我负责：
1. 决定做什么功能
2. 写需求描述给 AI
3. 刷新浏览器测试
4. 发现问题再让 AI 修
5. 检查代码是否合理
6. 用 Git 提交版本

---

## 五、踩坑经验（1.5 分钟）

### 坑一：AI 死循环烧光 2000 万 Token

这个是最惨的一次。我看 B 站博主让 AI 自己去百度查资料做文档，我也学着想让 AI 做 MCP 相关的内容。结果 AI 陷入死循环，不断搜索不断调用工具，一天烧光了 2000 万免费 Token。

教训：不能让 AI 无监督地执行搜索和工具调用。AI 没有"够用就停"的概念，必须人来控制边界。

### 坑二：环境配置地狱

配置 Claude Code 的过程中遇到了一堆问题：
- PowerShell 执行策略限制，改了 RemoteSigned 才能运行
- API Key 复制不完整，少了几个字符，导致 401 认证失败
- 中文输入乱码，需要 chcp 65001 设置 UTF-8 编码
- CC Switch 的余额查询功能跟智谱不兼容

这些问题现在看来很简单，但当时一个小白面对这些红色报错信息，真的会崩溃。

### 坑三：AI 说做了但其实没做

有好几次 Claude Code 告诉我"已经生成了饼图"，但我检查文件发现根本没写入。后来我学会了：AI 说做完之后，一定要自己去验证文件是否真的变了，不能光听它说。

---

## 六、总结与展望（30 秒）

### 核心收获

一个月前我连打字都要看键盘，现在能用 AI 工具开发一个完整的项目了。这个月我学到了三件事：

1. **基础很重要**：如果没第一周学的 Git、Markdown、命令行，后面根本做不下去
2. **AI 不是万能的**：它能写代码，但方向和质量得人来把控
3. **踩坑是最好的老师**：烧光 Token、配置失败、文件没写入——每个坑都让我学到了东西

### 未来方向

如果继续完善，想加入：后端数据同步、更多题库分类、AI 智能出题、学习计划推荐。也希望以后能像学长一样，用服务器和域名，严格按公司化流程做项目。

---

## 附：可能被问到的问题

**Q：你一个月前才开始学，怎么做到能开发项目的？**
A：说实话代码不是我手写的，是 AI 写的。但我学了 Git、Markdown、命令行这些基础，能看懂代码逻辑，能测试功能，能描述需求。我的角色更像产品经理，AI 是程序员。

**Q：为什么不用 React/Vue？**
A：我刚开始学，还没学到框架。纯 HTML/CSS/JS 是最基础的，先把这个学扎实。而且不引入框架的话，打开 HTML 就能用，零依赖零等待。

**Q：localStorage 有什么局限？**
A：容量有限，大概 5-10MB。不能跨设备同步，清除浏览器数据会丢失。如果要解决，可以加后端存储，比如 Node.js + 数据库。

**Q：题库数据内嵌在 JS 里，扩展性怎么样？**
A：50 道题大概 15KB，加载几乎无感。如果扩展到上千题，可以改为按分类懒加载 JSON 文件，或者用 IndexedDB 做本地缓存。这个我查过资料，知道思路但目前还没实现。

**Q：动画性能怎么保证不卡？**
A：用 CSS transform 和 opacity 做动画，不触发浏览器重排，GPU 会加速。动画结束后的 DOM 元素及时清理。这个是 AI 建议的，我查了资料理解了原理。

**Q：AI 写的代码质量怎么样？**
A：大部分逻辑是对的，但需要人工审查。比如它有时候说写了文件但实际没写入，要自己去验证。性能优化方面基本都是我提出方向，AI 负责实现。

**Q：MCP 是什么？**
A：模型上下文协议，让 AI 能访问外部系统，比如文件系统、数据库。相当于给 AI 装了"手"，能操作电脑上的文件。

**Q：Skill 是什么？**
A：可复用的提示词模板，类似函数。写好后用一句话就能触发一整套流程。

**Q：这个过程中最难的是什么？**
A：不是写代码，是环境配置。一个小白面对 PowerShell 的红色报错、API 认证失败、中文乱码，每个问题都要花很久才搞明白。但解决了之后特别有成就感。
