# AI 面试题练习器 — 答辩讲稿

> 总时长：约 8-10 分钟 | 语气：真实、朴素、小白视角

---

## 一、开场（40 秒）

各位老师好，我是 XXX。

说实话，一个月前我对计算机专业几乎没什么了解。8 月 17 号开始，我才第一次认真学习打字指法，第一次安装 VS Code，第一次知道什么是 Markdown。然后学长给了我一堆学习资料，让我从了解"计算机专业到底是什么"开始，一步一步学。

今天给大家展示的「AI 面试题练习器」，就是我在这个月学完基础之后，用 AI 编程工具做的第一个项目。虽然代码不是我一行一行手写的，但做什么、怎么做、对不对，都是我来决定的。

---

## 二、我的学习经历（1 分钟）

一个月前的 8 月 17 号，我刚开始学打字指法，第一次安装 VS Code，第一次知道什么是 Markdown。第一周了解计算机专业的各个方向，学了 Git、Markdown 这些基础。

第二周用 VitePress 框架搭了个人博客，第一次接触"项目"的概念——装 Node.js、npm 依赖、搭脚手架、写文章，最后用 GitHub Pages 部署上线，还写了 GitHub Action 实现自动部署。

第三周学长给了新任务：学习 Claude Code 等 AI 编程工具，开发一个作品答辩。

---

## 三、AI 工具学习过程（1 分钟）

Claude Code 是一个 AI 编程工具，用自然语言跟它说话就能生成代码，比如"帮我加一个搜索功能"。跟 ChatGPT 的区别是它能直接读写电脑上的文件。

第一个坑是网络问题，国内访问不了 Claude 服务器。最后用智谱开放平台的 GLM 模型替代，通过国内 API 端点访问，不需要翻墙还有免费额度。配置过程中踩了一堆坑——API Key 复制不完整导致认证失败、PowerShell 执行策略限制、中文乱码，但都一个个解决了。

另外还学了两个概念：MCP 是让 AI 能访问外部系统的协议，相当于给 AI 装了"手"；Skill 是可复用的提示词模板，类似函数。

---

## 四、项目开发与性能优化（2.5 分钟）

### 为什么做这个项目？

学长说"按你的想法 vibe 一个作品"。我正在准备面试，就做了面试题练习器，50 道题覆盖 Java 基础、并发、JVM、Spring、数据库五个分类。

### 性能优化（重点讲）

虽然我是小白，但开发中学到了一些性能知识：

**数据层：零网络请求。** 50 道题直接写在 JS 文件里，不用 fetch 请求外部文件。这样避免了 CORS 跨域问题，也消除了网络延迟，打开就能用。

**本地存储：localStorage。** 错题、收藏、连击、积分、答题用时全存在浏览器本地，毫秒级读取，不需要后端，刷新不丢失。

**动画优化：用 transform 不用 top/left。** CSS 动画用 transform 和 opacity 不触发浏览器重排，会被 GPU 加速。这个是 AI 建议的，我查资料理解了原理。

**计时器防泄漏。** 每次进入新题目前先 clearInterval 清除上一个计时器，防止内存泄漏。

### 功能快速演示

- 点击"开始答题"进入顺序答题，红色闹钟计时器实时计时
- 答对弹贴纸动画，连对 3 题触发撒花特效
- 答错弹安慰贴纸，连错 3 题全屏变灰抖动
- 错题本自动记录，统计页有正确率饼图、积分等级

### 公司化开发流程

这个项目我是按照公司化流程来推进的，不是想到哪做到哪。开发前先写了 PRD 产品需求文档，明确了功能范围和优先级。然后用 CLAUDE.md 定义了架构设计和技术规范。开发完通过 Git 提交版本管理，每次推送代码后 GitHub Action 自动检查文件结构、语法、文档完整性。整个过程 AI 负责写代码，我负责需求决策、测试验证、代码审查，更像是产品经理和程序员的关系。

---

## 五、踩坑经验（1 分钟）

**坑一：AI 死循环烧光 2000 万 Token。** 我学着 B 站博主让 AI 自动搜索网页做文档，结果 AI 陷入死循环，一天烧光了 2000 万免费 Token。教训：不能让 AI 无监督地执行搜索，必须人来控制边界。

**坑二：环境配置地狱。** PowerShell 执行策略限制、API Key 复制不完整导致认证失败、中文乱码，每个问题都要花很久。当时面对红色报错真的会崩溃，但一个个解决了。

**坑三：AI 说做了但实际没做。** 有好几次 Claude Code 说"已生成了饼图"，检查文件发现没写入。后来我学会了：AI 说做完一定要去验证，不能光听它说。

---

## 六、总结与展望（30 秒）

### 核心收获

一个月前我连打字都要看键盘，现在能用 AI 工具开发一个完整的项目了。这个月我学到了三件事：

1. **基础很重要**：如果没第一周学的 Git、Markdown、命令行，后面根本做不下去
2. **AI 不是万能的**：它能写代码，但方向和质量得人来把控
3. **踩坑是最好的老师**：烧光 Token、配置失败、文件没写入——每个坑都让我学到了东西

### 未来方向

如果继续完善，想加入：后端数据同步、更多题库分类、AI 智能出题、学习计划推荐。自动化测试目前还没做，后续可以引入单元测试框架。也希望以后能像学长一样，用服务器和域名，严格按公司化流程做项目。

---

## 附：可能被问到的问题

**Q：你一个月前才开始学，怎么做到能开发项目的？**
A：说实话代码不是我手写的，是 AI 写的。但我学了 Git、Markdown、命令行这些基础，能看懂代码逻辑，能测试功能，能描述需求。我的角色更像产品经理，AI 是程序员。

**Q：为什么不用 React/Vue？**
A：我刚开始学，还没学到框架。纯 HTML/CSS/JS 是最基础的，先把这个学扎实。而且不引入框架的话，打开 HTML 就能用，零依赖零等待。

**Q：localStorage 有什么局限？**
A：容量有限，大概 5-10MB。不能跨设备同步，清除浏览器数据会丢失。如果要解决，可以加后端存储，比如 Node.js + 数据库。

**Q：题库数据内嵌在 JS 里，扩展性怎么样？**
A：50 道题大概 15KB，加载几乎无感。如果扩展到上千题，可以改为按分类懒加载 JSON 文件，或者用 IndexedDB 做本地缓存。这个我查过资料，知道思路但目前还没实现。

**Q：动画性能怎么保证不卡？**
A：用 CSS transform 和 opacity 做动画，不触发浏览器重排，GPU 会加速。动画结束后的 DOM 元素及时清理。这个是 AI 建议的，我查了资料理解了原理。

**Q：AI 写的代码质量怎么样？**
A：大部分逻辑是对的，但需要人工审查。比如它有时候说写了文件但实际没写入，要自己去验证。性能优化方面基本都是我提出方向，AI 负责实现。

**Q：MCP 是什么？**
A：模型上下文协议，让 AI 能访问外部系统，比如文件系统、数据库。相当于给 AI 装了"手"，能操作电脑上的文件。

**Q：Skill 是什么？**
A：可复用的提示词模板，类似函数。写好后用一句话就能触发一整套流程。

**Q：这个过程中最难的是什么？**
A：不是写代码，是环境配置。一个小白面对 PowerShell 的红色报错、API 认证失败、中文乱码，每个问题都要花很久才搞明白。但解决了之后特别有成就感。

**Q：你说的公司化流程具体指什么？**
A：需求分析阶段写了 PRD 文档，明确功能范围和优先级；架构设计阶段写了 CLAUDE.md 定义技术规范；开发阶段用 Git 做版本管理；测试阶段手动验证每个功能；最后配了 GitHub Action 实现 CI/CD，每次推送自动检查代码质量。整个流程是需求→设计→开发→测试→文档→持续集成。

**Q：GitHub Action 做了什么？**
A：每次 push 代码到 main 分支，会自动触发 8 个检查步骤：文件结构完整性、HTML 标签、CSS 括号匹配、JS 语法、图片资源、文档完整性、代码行数统计。通过后显示绿色勾，失败显示红色叉。
